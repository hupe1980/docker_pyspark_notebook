# docker_pyspark_notebook

> Docker Compose setup for PySpark

## Start

- Download or clone this repository
- Run `docker-compose up` in the root directory
- Go to `http://localhost:8888/token=pyspark` in your browser
